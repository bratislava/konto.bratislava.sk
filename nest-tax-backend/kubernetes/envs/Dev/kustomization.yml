resources:
  - ../../base
  - ./secrets/cognito.secret.yml
  - ./secrets/reporting.secret.yml
  - ./secrets/email.secret.yml
  - ./secrets/admin.secret.yml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: env
  namespace: ${NAMESPACE}
  behavior: merge
  envs:
  - .env
