resources:
  - ../../base
  - ./secrets/cognito.secret.yml
  - ./secrets/azure-magproxy.secret.yml
  - ./secrets/bloomreach-contact-database.secret.yml
  - ./secrets/oauth2-dpb.secret.yml
  - ./secrets/oauth2-paas-mpa.secret.yml
  - ./secrets/noris.secret.yml
  - ./secrets/enforcement.secret.yml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ${BUILD_REPOSITORY_NAME}-env
    namespace: ${NAMESPACE}
    behavior: merge
    envs:
      - .env
