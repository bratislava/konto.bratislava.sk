resources:
  - ../../base
  - ./secrets/cognito.secret.yml
  - ./secrets/sharepoint.secret.yml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ${BUILD_REPOSITORY_NAME}-env
    namespace: ${NAMESPACE}
    behavior: merge
    envs:
      - .env
