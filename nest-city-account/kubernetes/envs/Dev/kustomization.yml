resources:
  - ../../base
  - secrets/database.secret.yml
  - ./secrets/cognito.secret.yml
  - ./secrets/oauth2-dpb.secret.yml
  - ./secrets/oauth2-mpa.secret.yml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ${BUILD_REPOSITORY_NAME}-env
    namespace: ${NAMESPACE}
    behavior: merge
    envs:
      - .env
